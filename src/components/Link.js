import styled from 'styled-components';

const Link = styled.div`
  margin-top: 60px;
  margin-bottom: 80px;

  color: #ffdc73;
  font-size: 1.2em;

  text-shadow: 0 8px 8px rgba(0, 0, 0, 0.5);
`;

export default Link;
